# Admin password for the legacy admin panel
ADMIN_PASSWORD=your-secure-password-here

# Secret key for legacy token signing (change in production!)
TOKEN_SECRET=your-secret-key-here

# Data directory for storing entries and photos
DATA_DIR=.data

# SQLite database path (local development)
DATABASE_URL=file:.data/data.db

# Turso database (production) — leave empty for local SQLite
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# JWT signing secret for owner authentication (change in production!)
JWT_SECRET=your-jwt-secret-here
