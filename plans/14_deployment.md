# Deployment ⚠️

Deploy to Vercel.

## Not Implemented

- [ ] No `vercel.json` configuration
- [ ] No Vercel project linked
- [ ] No deployment scripts beyond default `pnpm build`

Note: The project builds successfully with `pnpm build` (Nitro/Node output). Vercel deployment configuration has not been created yet.
