# API Development ✔

Create server routes for guest entries.

## Completed

- [x] `GET /api/entries` — List all guest entries
- [x] `POST /api/entries` — Create new guest entry (name, message, optional photo)
- [x] `GET /api/entries/[id]` — Get single entry by ID
- [x] `DELETE /api/entries/[id]` — Delete entry by ID
- [x] `GET /api/photos/[filename]` — Serve photo files with MIME type and cache headers
- [x] Server utility: `server/utils/storage.ts` for file I/O
