# Guest Form ✔

Define fields, validation and UX.

## Completed

- [x] `GuestForm.vue` component with name, message, and photo fields
- [x] Input validation via `useGuestForm` composable
- [x] Error display per field (destructive color)
- [x] Submit button with loading/disabled states
- [x] Photo upload integration via `PhotoUpload` component
- [x] Card-polaroid styling with display font heading
