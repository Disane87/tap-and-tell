# State Management ✔

Handle form state and submission lifecycle.

## Completed

- [x] `useGuests` composable — fetch, create, delete entries with reactive state
- [x] `useGuestForm` composable — form state, validation (name: 100, message: 1000, photo: 5MB), submission lifecycle
- [x] `useTheme` composable — light/dark/system theme with localStorage persistence
- [x] `useNfc` composable — NFC context detection from query params
- [x] `useAdmin` composable — admin auth with sessionStorage token
